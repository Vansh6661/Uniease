{
  "buildCommand": "bash -c 'cd frontend && npm run build'",
  "installCommand": "bash -c 'cd frontend && npm install --legacy-peer-deps'",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
